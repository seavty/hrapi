@{
    Layout = "~/Views/Shared/Main/_Main.cshtml";
}

@using HRWeb.Utils
@model HRApi.Models.DTO.GetListDTO<HRApi.Models.DTO.Employee.EmployeeWorkHistory>


@foreach (var item in Model.results)
{
    <div class="card" style="margin-bottom:5px">
        <div class="card-body">
            <div> @item.fromDate - @item.toDate</div>
            <div> @item.FrPosition - @item.ToPosition </div>
        </div>
    </div>
}